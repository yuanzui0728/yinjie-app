# Default provider for the instance
DEEPSEEK_API_KEY=
OPENAI_BASE_URL=https://api.deepseek.com
AI_MODEL=deepseek-chat

# Auth and server
JWT_SECRET=
ADMIN_SECRET=
PORT=3000
DATABASE_PATH=./data/database.sqlite

# Remote deployment
CORS_ALLOWED_ORIGINS=http://localhost:5180,http://localhost:5173
PUBLIC_API_BASE_URL=http://localhost:3000

# Encrypts per-user custom API keys on the server.
# Use a long random string in production. If omitted, JWT_SECRET is used as a fallback.
USER_API_KEY_ENCRYPTION_SECRET=
